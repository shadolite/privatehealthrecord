export enum ROUTE {
  // WELCOME = "/",
  DASH = "/dashboard",
  // ABOUT = "/about",
  // MOTD = "/motd",
  // LOCALIZATION = "/localization",
  // UNDOREDO = "/undoredo",
  // CONTEXTMENU = "/contextmenu",
  // IMAGE = "/image",
}
